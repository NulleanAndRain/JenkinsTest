pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                bat "build.bat"
            }
        }

        stage('Tests') {
            steps {
                mstest testResultsFile:"**/*.trx", keepLongStdio: true
            }
        }
    }
}
