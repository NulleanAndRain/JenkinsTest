pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                bat "build.bat"
            }
        }

        stage('Tests') {
            stetps {
                mstest testResultsFile:"**/*.trx", keepLongStdio: true
            }
        }
    }
}
