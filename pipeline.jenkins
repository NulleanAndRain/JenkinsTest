pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                bat "build.bat"
            }
        }

        stage('Tests') {
            steps {
                bat "tests.bat"
            }
        }
    }
}
